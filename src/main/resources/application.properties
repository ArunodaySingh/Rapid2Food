server.port=9090

# Db configuration
spring.datasource.url=jdbc:mysql://localhost:3306/rapid2food
spring.datasource.username=root
spring.datasource.password=//Enter your password
# if use another database then driver class name also change
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Hibernate dialect is change according to database  we can check all dialect using ctrl+shift+T 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# create ,update , create-drop , validate
spring.jpa.hibernate.ddl-auto=update
# if necessary then update

spring.jpa.show-sql=true
# it show the sql when server run
